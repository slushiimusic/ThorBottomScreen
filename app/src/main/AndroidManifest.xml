<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Shizuku supplies the shell identity we need to write the vendor's screen-mode key.
         SettingsProvider refuses that write from any non-system uid, so WRITE_SETTINGS and
         even WRITE_SECURE_SETTINGS were both measured to be useless here. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!-- The no-Shizuku fallback: lets us put our own black window over the bottom screen,
         which is the same thing the vendor's own "top display only" does. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Optional, and only for the rotation-suggestion option. Not held by default; it can
         be granted once over adb and then survives reboots, unlike Shizuku:
         adb shell pm grant dev.slushii.thorbottom android.permission.WRITE_SECURE_SETTINGS -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Needed to list installed apps so the user can pick which ones disable the bottom screen. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The entire runtime of this app. An AccessibilityService is kept alive by the
             system for free and is woken only when a window actually changes, so the app
             costs nothing while idle: no polling, no timers, no overlay, no wake locks. -->
        <service
            android:name=".AppMonitorService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>
    
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
